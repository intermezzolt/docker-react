sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t intermezzolt/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true intermezzolt/docker-react npm run test -- --coverage
